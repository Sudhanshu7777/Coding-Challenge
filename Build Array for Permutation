class Solution(object):
    def buildArray(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        # Create a new list 'ans' where each ans[i] = nums[nums[i]]
        ans = [nums[nums[i]] for i in range(len(nums))]
        return ans
